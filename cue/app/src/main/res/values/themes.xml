<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Cue" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- The share target renders nothing of its own: it ingests and hands off,
         so a visible window would flash for the length of an OCR pass. -->
    <style name="Theme.Cue.Transparent" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
