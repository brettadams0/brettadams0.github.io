// Microphone acquisition. getUserMedia with echoCancellation, noiseSuppression and autoGainControl forced off, verified via getSettings(). (Phase 1)
//
// Placeholder. Phase 0 creates the file; the phase named above fills it.
export {};
