// Mic capture, AudioContext lifecycle, permission. getUserMedia with echoCancellation, noiseSuppression and autoGainControl false, verified via getSettings(). (Phase 1)
//
// Placeholder created in Phase 0. The phase named above fills it.
export {};
